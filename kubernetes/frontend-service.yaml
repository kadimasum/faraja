apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: faraja
  labels:
    app: faraja
    tier: frontend
spec:
  selector:
    app: faraja
    tier: frontend
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  type: LoadBalancer
